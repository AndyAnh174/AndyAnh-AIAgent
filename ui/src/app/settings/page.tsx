import { SettingsSection } from "@/components/sections/settings-section";

export default function SettingsPage() {
  return (
    <main className="flex flex-col items-center min-h-screen w-full py-8">
      <SettingsSection />
    </main>
  );
}
